local MovementConfig = {
    -- Base movement tuning used by client controllers.
    walkSpeed = 16,
    sprintSpeed = 22,
    acceleration = 10,
    deceleration = 14,
    minMovingMagnitude = 0.05,
    inputActionPriority = 2000,
}

return MovementConfig
